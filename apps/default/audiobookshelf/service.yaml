apiVersion: v1
kind: Service
metadata:
  name: prowlarr
  namespace: default
  annotations:
    argocd.argoproj.io/sync-wave: "50"
spec:
  selector:
    app: prowlarr
  ports:
    - targetPort: 80
      port: 80
